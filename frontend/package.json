{
    "name": "weather-app-frontend",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "format": "prettier --write src",
        "lint": "eslint . --ext .js,.jsx"
    },
    "devDependencies": {
        "prettier": "^3.5.3"
    }
}
